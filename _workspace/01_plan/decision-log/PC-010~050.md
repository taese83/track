# Decision Log — PC-010~050

## PC-010 · 뷰어 조작 결함 3건 + 높이 정정 (2026-08-29, 사용자 지적 3건)

사용자 지적: ①"움직임이 너무 느린데 프리뷰라서 그런거지?" ②"x y z 축 360도로"
③"슬로프 높이가 조금 과해". ①은 **프리뷰 탓이 아니었다**.

### ① 드래그가 느린 게 아니라 아무것도 안 그리고 있었다 — 내가 만든 회귀

측정부터 했다. 재도색 1회 = **중앙값 3.2ms**(60fps 예산 16.7ms의 1/5). 성능 문제가 아니다.

원인은 `renderCanvasOnlyOrbit()`이 **빈 함수**였다는 것. 3D 재작업(PC-008) 때 종전
`g.setAttribute('transform', rotate(...))` 한 줄을 지우면서 주석만 남기고 대체 구현을
넣지 않았다. 패치 스크립트의 단계 이름이 문자 그대로 **"orbit-only 갱신 무력화"**였다 —
무력화해 놓고 복구를 잊었다.

증상이 "안 움직임"이 아니라 "느림"으로 보인 이유: 방향키는 `render()`(전체 재렌더)를
불러서 동작했고, 드래그만 죽어 있었다.

**수정** — SVG 요소는 유지하고 내용만 다시 그린다. 요소를 통째로 갈아끼우면
(`renderViewerBody()`) 드래그 중 pointer capture와 리스너가 끊긴다:
`paint()`를 `svgEl.__whRepaint`로 노출하고, `renderCanvasOnlyOrbit()`은 rAF로 프레임당
1회만 호출한다. 방향키·휠·`+`/`-`도 전체 재렌더에서 이 경량 경로로 옮겼다 —
회전·확대는 캔버스 밖 표면을 바꾸지 않는다.

**딸려 나온 결함**: 드래그 중 구간 집기가 `nearestIndexByXY(segs, rx, ry)`로
**편집기 원좌표**와 비교하고 있었다. 3D 재작업으로 `<g transform="rotate">`가 사라졌는데
역회전 보정 코드는 남아 있었다. 투영 좌표 기준(`__whCenters`)으로 교체했다.

### ② x축이 0~89도로 잠겨 있었다

`Math.max(0, Math.min(89, ...))`. 트랙을 **아래에서 올려다볼 수 없었다.**
세 축 모두 `wrap360`으로 바꿨다. 조작 안내 문구에 축을 명시했다(종전 "방향키: 회전"만
있어 어느 키가 어느 축인지 알 수 없었다).

검증(6도씩 60회 = 한 바퀴): x 58→118→178→238→298→358→58, 서로 다른 각도 60개,
**전 단계에서 132면 유지**. y는 0→90→180→270→0, z는 0→120→240→0으로 순환.

### ③ 높이 → D-023 (별도 엔트리)

`tan` → `sin` 정정. 슬로프 45.3 → **34.7cm**(−23%), 뱅크 10.2 → **9.6cm**.
근거와 파생 문서 갱신은 `D-023~D-030.md` 참조.

### 검증 (수정 후)

| 항목 | 값 |
|---|---|
| 재도색 1프레임 | 3.2ms (max 5.9) |
| 벌어진 이음새 | 7 / 131 (전부 순서 미확정 꼬리) |
| 고도 불연속 | 0건 |
| 최종 누적 z | 0.0 (폐합) |
| z 범위 | −34.7 ~ 0 |
| 3축 360도 | x·y·z 전부 순환 확인 |

### 하네스 관찰 (PC-009에 이어)

PC-009와 **같은 계열의 결함이 또 나왔다**: 재작업이 기존 동작을 조용히 죽였는데
게이트도 육안 검수도 잡지 못했다. 이번엔 내가 "무력화"라고 이름 붙인 패치 단계가
그대로 남았다는 점에서 더 나쁘다.

공통 성질: **없어진 동작은 화면에 아무 흔적을 남기지 않는다.** 잘못 그려진 것은 보이지만
안 그려진 것은 안 보인다. 프리뷰 검증이 스크린샷 중심인 한 이 계열은 계속 샌다.
필요한 것은 **동작 단언**(드래그 → 각도 변화, 이음새 연속성, 축별 360도 순환)이며,
이번에 쓴 측정들이 그대로 그 단언의 초안이다.

## PC-011 · 뱅크 모델을 세 번 고쳐 쓴 과정 (2026-08-29)

한 번에 맞히지 못했다. 기록해 둘 값어치가 있는 것은 **무엇이 판정을 갈랐는가**다.

| 회차 | 구현 | 사용자 반응 |
|---|---|---|
| 1 | 롤 0→20° 로그 전이 + 이후 유지 | "뱅크 구현이 잘못되었어… 트랙이 휘어지는 것은 아니야" |
| 2 | 롤 20° 균일(피스 안 일정) | (A/B 비교 제시) → **C 선택** |
| 3 | **경사(pitch) 인계** — 롤 폐기 | 확정 |

근본 원인은 **"기울어지게"를 롤로 읽은 것**이다(D-024 오독). 사용자가 말한 20°는
좌우 눕힘이 아니라 진행 방향 경사였다. 1·2회차는 같은 오독 위에서 형태만 바꾼 것이라
둘 다 틀릴 수밖에 없었다.

### 무엇이 판정을 갈랐나

**말이 아니라 그림과 숫자다.**

- 2회차에서 A/B를 **나란히 렌더해 보여준 뒤에야** 방향이 잡혔다. 그 전까지는
  텍스트로 두 번 주고받으며 서로 다른 것을 가리키고 있었다.
- 3회차에서 "경사를 트랙 끝까지 이어간다" 해석은 **폐곡선이 132.3cm 어긋난다**는
  수치가 즉시 기각했다. 이웃 한 피스로 한정하니 0.0으로 닫혔다.
  **닫힘 여부가 해석을 판별하는 무료 검증기였다.**

### 반성

사용자 지적("잘못되었어")을 받고 **곧바로 두 번째 구현으로 넘어간 것이 성급했다.**
첫 지적 시점에 A/B를 그려 보여줬다면 왕복 한 번을 줄였다. 텍스트로 형상을 합의하려는
시도 자체가 비용이었다 — 형상 문제는 형상으로 물어야 한다.

또 롤 부호를 "혼재(9/13)"라고 보고했다가 정정했다. 롤이 걸리지 않은 피스까지 범위에
넣고 센 **측정 자체의 결함**이었다. 실제로는 8/8 전부 정상이었다. 측정 범위를 결과와
같은 기준으로 잡지 않으면 없는 결함을 만들어낸다.

### 부수 확인

- 롤이 폐기되면서 `ASSUMPTION-008`(롤 부호)은 소멸했다.
- 슬로프 각도는 D-025로 25°(피스당 22.8cm). 트랙 z 범위 −23.7~0.

## PC-012 · 구간 목록이 뷰어 높이를 넘긴 결함 — **PC-006의 재발** (2026-08-29, 사용자 지적)

> "텍스트 구간 목록은 뷰어 크기와 같은 높이로 하고 스크롤 추가해줘"

원인이 **둘**이었고, 하나가 다른 하나를 가리고 있었다.

### ① 스크롤 규칙이 한 번도 적용된 적이 없다 — 클래스 이름 불일치

```css
[role="listbox"].section-list { overflow-y: auto; flex: 1; }   /* app.css */
```
```js
class: 'wh-feat-section-list'                                   /* app.js — section-list가 없다 */
```

`app.js`는 앵커 클래스만 붙였다. **선택자가 한 번도 매치되지 않았고**, 132행이 그대로
펼쳐져 목록이 **4536px**가 됐다.

이것은 PC-006에 기록된 "실 트랙 132행에서 SVG가 4566px가 된 결함"과 **같은 결함**이다.
그때 CSS 쪽은 고쳤는데 클래스가 붙지 않아 실제로는 고쳐지지 않았다. 기록만 보면
닫힌 것으로 보인다.

### ② `#app` 높이가 배너 높이를 상수로 가정했다

```css
#app { height: calc(100vh - 40px); }
```

충실도 배너는 `#app` **바깥**에 있고, 폭이 좁으면 두 줄로 접혀 40px을 넘는다. 그만큼
문서가 세로로 넘쳐 **페이지 전체가 스크롤**됐다(문서 4732px / 창 578px).

`body`를 flex 칼럼으로 두고 `#app`이 **남는 높이 전부**를 받게 바꿨다. `overflow-y`만
잠가 가로 스크롤(min-width 960px 규칙)은 유지된다.

### 실측

| 항목 | 전 | 후 |
|---|---|---|
| 문서 높이 / 창 높이 | 4732 / 578 (페이지 스크롤) | **578 / 578 (스크롤 없음)** |
| 목록 패널 높이 | — | **223px = 캔버스 223px** |
| 목록 내부 스크롤 | 없음 (4536px 펼침) | **있음** (clientH 177 / scrollH 4536) |

### 하네스 관찰 (PC-009·PC-010에 이어)

**같은 계열이 세 번째다.** 공통 성질은 여전하다 — *없어진 동작은 화면에 흔적을 남기지
않는다.* 이번엔 한 걸음 더 나쁘다: **결함이 "고쳐진 것으로 기록돼 있었다."** CSS 규칙은
정확했고 결정 로그도 남아 있었는데, 그 규칙을 받을 클래스가 마크업에 없었다.

필요한 단언은 스크린샷이 아니라 **선택자가 실제로 매치되는지**다. 이번에 쓴
`document.querySelector('.section-list')`가 null이었다는 사실이 그 단언의 초안이다.
`data-wh-anchor`가 붙은 요소는 스타일 클래스도 함께 검사해야 한다.


## PC-013 (2026-08-31) — 스트립·자동재생 조작 시 구간 목록 자동 스크롤·roving 포커스 동기화
- 트리거: 기능 변경 (/feature-add "상대 스케일 포커스 시에 동일한 구간 목록으로 스크롤되고 포커스")
- 대상: FEAT-013, 화면 3D 표시(TrackScreen)·WebGL 미지원 대체 화면
- 변경: 스트립 스크럽/키보드·자동재생(canvas)으로 공유 커서가 바뀌면 목록이 해당 행으로 스크롤(block: nearest)되고 그 행이 roving 포커스가 된다. DOM 포커스는 목록이 이미 포커스를 가질 때만 이동(기존 규칙). source `list`는 스크롤을 건드리지 않는다. 종전에는 강조(aria-selected)만 되고 132행 목록이 스크롤되지 않아 강조 행이 화면 밖에 남았다.
- 근거·승인: 사용자 승인 (Phase 1 체크포인트, 2026-08-31 — "스트립+자동재생 모두")
- 영향 산출물: feature-plan specs-surfaces.md(FEAT-013 TC-013-6 신설), component-spec widgets.md §SectionList·pages.md 상호작용 표(DOCS_TO_UPDATE), requirements 변경 없음(REQ-F-024 범위 내 동작 정밀화)
## PC-014 (2026-09-02) — 프로파일 스트립 인디케이터의 연속 진행축 (플라이스루 연동)
- 트리거: 기능 변경 (`/wh "프로파일 스트립에 현재 위치 마커 — 플라이스루와 연동"`)
- 대상: FEAT-012 (인디케이터), FEAT-007이 발행원. 화면 3D 표시(TrackScreen)
- 관측된 baseline: 마커와 플라이스루 연동은 **이미 성립해 있었다** — `FlythroughRig`가 재생 중
  `setCursor(order,'canvas')`로 공유 커서를 밀고 스트립이 그 커서를 읽어 세로선을 그린다. 다만 축이
  구간 단위 하나뿐이라 계단식으로 튄다. 브라우저 실측(2026-09-02, WS67Y2 132피스, 240cm/s): 재생 후
  550ms 동안 카메라는 110cm를 갔는데 마커 x는 12px에 고정, 다음 표본에서 31px로 두 칸을 건너뛰었다.
- 변경: 인디케이터에 **연속 진행축**을 겹친다. 자동 재생 중(seek 중이 아닐 때) 카메라의 누적 거리에서
  파생한 소수 인덱스(`order + t`)를 리렌더 없는 별도 채널로 스트립에 전달하고, 스트립은 인디케이터
  하나의 transform만 프레임마다 갱신한다. 공유 커서의 발행 빈도·의미는 그대로다(구간 단위) —
  목록 스크롤·roving 포커스(TC-013-6)·카메라 목표·`aria-valuenow`는 계속 공유 커서만 본다.
- 대안과 기각 사유: (a) 공유 커서 자체를 소수 인덱스로 승격 — 세 표면이 60fps로 재렌더되고 132행
  목록이 매 프레임 스크롤 계산을 한다(component-spec §공유 커서 계약의 순환·스톰 방지와 정면 충돌).
  (b) 기존 `data-follow-distance`(100ms 주기 DOM 발행)를 스트립이 읽기 — 10Hz는 여전히 눈에 띄게
  끊기고, 검증용 관측 표면을 렌더 입력으로 승격하면 그 표면을 못 바꾸게 된다.
- 근거·승인: 사용자 선택 (2026-09-02, `/wh` 레인 확인 시 "연속 마커로 매끄럽게")
- 영향 산출물: feature-plan specs-surfaces.md(FEAT-012 명세 + TC-012-6 신설)·traceability.md,
  component-spec shared.md §공유 커서 계약(연속 진행 채널 신설)·widgets.md §ProfileStrip/§TrackCanvas,
  performance-budget targets.md(플라이스루 fps 회귀 가드 명시). requirements 변경 없음
  (REQ-F-023 범위 내 동작 정밀화).
## PC-015 (2026-09-02) — 목록·스트립이 고른 구간을 3D 씬이 하이라이트한다
- 트리거: 기능 변경 (`/wh "프로파일이나 구간 목록을 클릭하면 실제 트랙에서 어느 위치인지 하일라이트 하는 기능 추가"`)
- 대상: FEAT-019 신설. 소비 표면은 `TrackCanvas`, 발행원은 기존 공유 커서(FEAT-012·013). 화면 3D 표시·부분 실패
- 관측된 baseline: 클릭 → 공유 커서 이동 → 목록 강조·스크롤·스트립 인디케이터까지는 **이미 성립해 있었다**
  (PC-013·PC-014). 빠진 것은 캔버스다 — `TrackCanvas.tsx`가 `currentIndex`를 읽는 곳은 추종 카메라
  목표를 정하는 `useEffect` 하나뿐이고 그 첫 줄이 `if (!following …) return`이다. 추종을 끈 기본
  오빗 시점에서 목록·스트립을 눌러도 3D 씬은 그대로다. REQ-F-023·REQ-F-024 원문의 "3D 뷰가 그
  지점으로 이동한다"가 추종 ON에서만 참이었다.
- 변경: 커서가 가리키는 구간에 `primary` 반투명 면 오버레이 + 윤곽선을 얹는다. 윤곽선은 깊이 검사를
  하지 않아 가려진 구간도 위치가 보이고, 면은 깊이 검사를 유지해 "가려져 있다" 자체가 남는다.
  카메라는 **대상이 뷰 절두체 밖일 때만** 타깃을 옮기고 방위각·극각·거리는 보존한다. 추종 중에는
  하이라이트를 그리지 않는다.
- 사용자 선택 (2026-09-02, `/wh` 레인 확인 시 3문항): ① 표시 형태 = **면 강조 + 윤곽선**
  (비컨·라벨 기각 — 씬 요소를 늘리지 않는다 / 윤곽선 단독 기각 — 멀리서 안 띈다)
  ② 카메라 = **화면 밖일 때만 이동**(항상 이동 기각 — 가까운 구간 연속 클릭 시 화면이 계속 흔들린다 /
  이동 없음 기각 — 132구간 트랙에서 화면 밖 구간이 안 보인다)
  ③ 추종 중 = **숨김**(계속 표시 기각 — 1인칭 시야 바로 앞을 보라색 면이 덮는다)
- 대안과 기각 사유: (a) 표면색 자체를 `primary`로 치환 — rise/fall 원본 편집기색 대조가 사용자
  1순위 성공 조건이라(design-system §1) 그 구간에서 대조가 통째로 사라진다. 덧칠로 둔다.
  (b) 연속 진행축(PC-014) 구독 — 하이라이트는 구간 단위이고 추종 중에는 아예 숨기므로 소용이 없다.
  구독자를 늘리면 shared.md §연속 진행 채널의 "구독자는 하나뿐"이 깨진다.
  (c) 캔버스가 커서를 되쓰기(카메라 이동 후 `setCursor`) — 1차 게이트 위반이자 A→B→A 경로 신설.
- 색 근거: 새 토큰을 만들지 않았다. `primary #A78BFA`의 정의 근거가 이미 "액션/포커스/**현재 위치
  마커**. 보라인 이유: 편집기 피스 팔레트(빨강·파랑·청록·주황)와 겹치지 않는 유일한 안전 hue"다
  (design-system tokens §2). 3D 표면 소비는 이번에 처음이라 §1 표면 분리 계약에 그 줄을 더한다.
- 영향 산출물: feature-plan feature-list.md(FEAT-019 행)·specs-scene.md(명세 + TC-019-1~7)·
  traceability.md, component-spec widgets.md §TrackCanvas·pages.md 상호작용 표·shared.md §공유 커서,
  design-system tokens.md §1·inventory.md·accessibility.md, layout-spec states.md §3D 표시,
  performance-budget targets.md. requirements 변경 없음(REQ-F-023·024 범위 내 미충족분 완결).
## PC-016 (2026-09-03) — 하이라이트 테두리를 이중 톤·실제 두께로 (가시성)
- 트리거: 기능 변경 (`/wh "하일라이트 색상을 좀더 가시성있도록 변경"`)
- 대상: FEAT-019(테두리 채널). 화면 3D 표시·부분 실패
- 관측된 baseline(2026-09-03 대비 실측): PC-015가 넣은 `primary #A78BFA` 1px 윤곽선의 **평지 노면
  대비가 1.10:1**이다 — WCAG 비텍스트 3:1의 3분의 1. 면 오버레이(@0.45)도 평지에서 1.10.
  원인은 **색조가 아니라 명도**다: `#A78BFA`와 평지 `#A8AEB8`의 상대 휘도가 거의 같다.
- 핵심 발견: **어떤 단일 색도 세 노면 모두에 3:1을 낼 수 없다.** 밝은 평지(#A8AEB8)에는 어두워야
  하고 어두운 상승(#AD0A09)·하강(#004E8F)에는 밝아야 한다. 실측으로 확인 — 진한 보라 `#6D28D9`는
  평지 1.99를 얻는 대신 상승 1.12·하강 1.01로 **오히려 사라진다**. 색 하나를 더 잘 고르는 문제가
  아니었다.
- 변경: 테두리를 **두 톤**(밝은 `#C4B5FD` · 어두운 `#2E1065`)으로 나란히 두고, **선이 아니라 씬
  단위의 실제 폭을 가진 띠(면)**로 그린다. 두 띠 모두 구간 안쪽으로 깔아 이웃을 덮지 않는다.
  배경별 최댓값: 평지 6.83 · 평지가운데 7.98 · 상승 4.03 · 하강 4.57 · 미지원 3.29 · 씬 배경 10.17.
- 두께가 필요한 이유: WebGL의 선 굵기는 대부분 플랫폼에서 1px 고정이라 `linewidth`가 무시된다.
  132피스 전체 보기에서 한 구간 윤곽선이 17px뿐이라(2026-09-02 실측) 대비만 고쳐도 얇으면 안 보인다.
- 사용자 선택 (2026-09-03, 3안 중): **이중 테두리 + 실제 두께**. 기각 — (a) 이중 선만(두께 없이):
  대비는 같이 해결되나 전체 보기에서 여전히 얇다. (b) 면 불투명도 0.85: 확실히 보이지만 그 구간의
  상승 빨강·하강 파랑이 사라져 "도면과 3D 나란히 대조"(1순위 성공 조건)를 그 자리에서 포기한다.
- 파생 2색을 **CSS 변수로 내지 않는다** — 소비자가 three 재질뿐이라 `var()`를 읽지 못한다.
  변수만 만들면 아무도 쓰지 않는 토큰이 팔레트에 남는다(`src/index.css`는 ALLOWED_PATHS 밖이기도 하다).
- 영향 산출물: feature-plan specs-scene.md(FEAT-019 명세 + TC-019-8 신설, TC-019-3 문구),
  component-spec widgets.md §TrackCanvas, design-system tokens.md §1·inventory.md·accessibility.md,
  layout-spec states.md, performance-budget targets.md(draw call 2→3).
  requirements 변경 없음(REQ-NFR-003·비텍스트 대비 계약 범위 내 결함 해소).
## PC-017 (2026-09-03) — 레인 경계에 5cm 트랙 벽
- 트리거: 기능 변경 (`/wh "트랙에 5cm의 트랙벽을 추가해야한다. 각 레인마다 벽이 있다."`)
- 대상: FEAT-020 신설 · REQ-F-025 신설. 화면 3D 표시·부분 실패
- 관측된 baseline: 씬에 벽 개념이 없다(`grep -rn "벽|wall" src/` = 플라이스루 주석 한 줄뿐).
  레인은 두께 없는 면 3장과 경계선 4줄로만 표현되어 형상이 "레인이 그려진 판"이다.
- 변경: 각 레인 경계에 노면 5cm 벽. 정본 수치는 `piece-geometry.md` §레인 경계에는 5cm 벽이
  선다에 둔다(FEAT-008 레인 기하 옆 — 같은 성격의 값이 흩어지지 않게).
- **질문 없이 정한 것**(이미 판정이 있거나 관례가 명확): 줄 수는 `boundaryLinesOf`와 같은 규칙
  (맞붙은 레인 4 · 명시 경로 레인 6), 5cm는 높이이고 두께는 0(레인 면과 동일), 미지원 피스는
  레인이 없으므로 벽도 없음. 추종 카메라 눈높이 12cm라 5cm 벽에 시야가 막히지 않는다.
- 사용자 선택 (2026-09-03, 3문항):
  ① 레인체인지 사이 벽 = **자리바꿈을 따라 같이 휘게 그린다**. 오케스트레이터는 "벽끼리
     교차한다"고 경고했으나 **그 경고가 과했다** — 이웃 레인은 붙은 채 미끄러지고 두 칸 건너뛰는
     레인만 육교로 8cm 떠오르므로 대부분 높이로 해소된다. 남는 위험은 `sin²` 오르내림 구간에서
     육교 바닥이 5cm 미만인 동안의 겹침이며, 기각된 대안은 "그 구간의 사이 벽 제거"다.
     **구현 후 실측으로 겹침이 확인됐다**(2026-09-03, `Lan1` 기하 20001 표본): 최대 돌출
     4.94cm, 가장 넓을 때 벽이 육교 면 6cm 안쪽에서 3cm 솟음. 사용자가 **"육교 밑을 지나는
     동안만 벽을 낮춘다"**를 선택해(2026-09-03) "밑동 위에 다른 레인 면이 있으면 그 바닥까지만
     세운다" 규칙을 넣었다 — 끊는 것이 아니라 낮아지는 것이라 통과 구간에서 벽이 생겼다 말았다
     하지 않는다. 같은 방식으로 재검산한 결과 **관통 0.000000cm**. 규칙은 레인체인지 여부를
     묻지 않으며 겹치지 않는 피스에는 영향이 없다.
  ② 뱅크 방향 = **노면에 수직**(같이 기운다). 기각: 세계 좌표 연직 — 기운 노면과 벽 사이에
     틈이 생기거나 벽이 노면을 파고든다.
  ③ 색 = **노면색의 어두운 파생**. 기각: 전 구간 동일 무채색 — 벽이 구간 유형 정보를 잃는다.
- 근거 등급: **높이 5cm는 `confirmed`이지 `measured`가 아니다.** 편집기 도면이 2D 평면도라
  픽셀 실측이 불가능하다 — 다른 치수(레인 폭 12cm 등)와 등급이 다르다는 사실을 숨기지 않는다.
- 완화 정책: 벽은 완화 수단 목록에 **넣지 않는다**(mitigation.md). 끄면 REQ-F-025가 요구한 것
  자체가 사라진다 — 화질 저하가 아니라 기능 제거다.
- 영향 산출물: requirements/functional.md(REQ-F-025 신설), feature-plan feature-list.md·
  specs-scene.md(FEAT-020 + TC-020-1~7)·traceability.md, piece-geometry.md(§벽 정본),
  component-spec widgets.md, design-system tokens.md·inventory.md,
  performance-budget targets.md·mitigation.md.
